# Runbook — {{WORKFLOW_NAME}}

> Generated by `/n8n-docs` on {{DATE}} from {{SOURCE}} (redacted). Re-run after significant changes.

## Workflow này làm gì

{{BUSINESS_SUMMARY}}

- **Kích hoạt bởi**: {{TRIGGER_DESCRIPTION}}   <!-- e.g. "Lịch: mỗi ngày 07:00 (giờ VN)" / "Webhook: POST /orders (auth: header)" -->
- **Tần suất**: {{CADENCE}}
- **Trạng thái**: {{ACTIVE_STATE}}   <!-- active / inactive -->

## Sơ đồ luồng

```mermaid
{{MERMAID_GRAPH}}
```

## Các bước (node)

| Node | Loại | Nhiệm vụ | Credential |
|---|---|---|---|
{{NODE_TABLE_ROWS}}

## Credentials sử dụng

{{CREDENTIAL_LIST}}   <!-- tên credential + loại, KHÔNG có giá trị bí mật -->

## Khi có lỗi

- Xem lỗi gần nhất: `n8nctl execution last-error --workflow {{WORKFLOW_ID}} --summary`
- Tự sửa (self-heal): `/n8n-fix {{WORKFLOW_ID}}`
- Khôi phục bản trước: `/n8n-rollback {{WORKFLOW_ID}}`

## Ghi chú vận hành

{{OPERATIONAL_NOTES}}   <!-- rate limits, external dependencies, giờ chạy nhạy cảm, contact người phụ trách -->
